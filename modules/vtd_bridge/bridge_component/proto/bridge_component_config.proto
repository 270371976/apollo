syntax = "proto2";

package apollo.vtd_bridge;

message VtdBridgeConfig {
  optional double ts = 1;
  optional string remote_ip = 2;
  optional uint32 remote_send_port = 3;
  optional uint32 remote_receive_port = 4;
};
